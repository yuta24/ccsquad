{
  "name": "ccsquad",
  "version": "0.3.0",
  "description": "ステートマシン型ワークフローエンジン CLI — Claude Code の計画→実装→評価ループを強制するハーネス",
  "type": "module",
  "bin": {
    "ccsquad": "./dist/ccsquad"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile src/index.ts --outfile dist/ccsquad"
  },
  "dependencies": {
    "yaml": "^2.7.0",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
